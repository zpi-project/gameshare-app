import { FC } from "react";
import { DialogContent } from "@/components/ui/dialog";

const GameReservations: FC = () => {
  return <DialogContent>GameInstanceCalendar</DialogContent>;
};

export default GameReservations;
