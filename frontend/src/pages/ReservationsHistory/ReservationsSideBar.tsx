import { FC } from "react";

const ReservationsSideBar: FC = () => {
  return (
    <div className="bg-section">
      <div className="bg-card">ReservationsSideBar</div>
    </div>
  );
};

export default ReservationsSideBar;
