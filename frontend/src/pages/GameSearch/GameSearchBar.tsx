import { FC } from "react";

const GameSearchBar: FC = () => {
  return (
    <div className="w-[500px] rounded-lg bg-card p-2">What board game would you like to play?</div>
  );
};

export default GameSearchBar;
